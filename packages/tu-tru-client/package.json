{
  "name": "@pseo/tu-tru-client",
  "version": "0.1.0",
  "description": "Thin client + input_data mapper for the tu-tru-api day-detail endpoint (generic mode). Used ONLY by offline worklist-building scripts — never imported by supabase/functions (the engine makes no live network calls).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test 'test/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
